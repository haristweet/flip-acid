# flip-acid — HANDOFF

最終更新: 2026-09-03 / 手元 v0.1.0・公開中は v0.0.1

- リポジトリ: `haristweet/flip-acid`
- 公開URL: https://haristweet.github.io/flip-acid/
- 一覧ページ: https://haristweet.github.io/ （`haristweet/haristweet.github.io`、現在 v1.7.2 / 21件）

---

## これは何か

8BitDo FlipPad で遊ぶことを前提にした、**TB-303 のフィルターをいじる時間だけを取り出したブラウザ玩具**。ゲームではない。

十字キーをツマミに見立てる。押した方向にツマミをひねったことになる。

シーケンサーは持たない。パターンを打ち込む機能は意図的に捨てた。「場所を選ばず、気張らずサクッと」という条件から、鳴っている音に対してツマミを回すだけの体験に絞る、という判断。

---

## 対象デバイス: 8BitDo FlipPad

- USB-C 有線接続。バッテリーもペアリングもない
- 縦持ち専用。コントローラが画面の下側に前面から被さる
- ヒンジで持ち上げると、ケーブルを抜かずにタッチスクリーンにアクセスできる
- D-pad / ABXY / Select / Start / L1 / L2 / R1 / R2
- **アナログスティックなし**
- L1・L2・R1・R2 は背面ではなく**前面**配置。D-pad + ABXY と同時に押すのは実用的でない可能性が高い（要実機確認）
- 70 × 66.5 × 19.7mm / 31.5g
- iOS 26.2 以上 / Android 13 以上
- 2026-09-03 時点で本体未着（中国から発送待ち）

---

## 決定済みの設計

### コントロール割り当て

| 入力 | 対象 |
|---|---|
| 十字キー 上下 | cutoff |
| 十字キー 左右 | resonance |
| 斜め入力 | cutoff と resonance を同時に動かす（実機の両手操作の再現） |
| L1 + 上下 | env mod |
| L1 + 左右 | decay |
| A | accent |
| B | slide |

L1 は「押しながら他を操作する」修飾キーなので、同時押しの負担が問題になるようなら再考する。

### ツマミの挙動

- **押しっぱなしで変化量が加速する**。等速だと数値入力になってしまい、ツマミにならない
- cutoff は**対数**で動かす（オクターブ/秒）。線形だと下が死んで上が飛ぶ
- ツマミは絶対位置、十字キーは相対入力。この齟齬を埋めるため、**4つのノブの現在角度とフィルターカーブのリアルタイム表示は機能として必須**。飾りではない

### 音

**実装済み（v0.1.0 / `acid-voice.js`）**。1 極 4 段のラダーを組み、4 段目を帰還に回して出力は 3 段目から取る。
18dB/oct でありながら 4 極ぶんの共振が乗る。帰還の `tanh` で自己発振と低域痩せが出る。

正弦波掃引の実測で傾き −18.6dB/oct、k=2.5 でピーク +7.2dB・DC −7.1dB。
画面のカーブは同じ伝達関数を描いていて、実測と 0.6dB 以内で一致する。

オシレータ・エンベロープ・シーケンサーも worklet の中に入れた。`setInterval` のゆらぎがタイミングに乗らない。

つまずいた点が 2 つある。どちらも直してある。

- 帰還に 1 サンプルの遅れがある以上、カットオフを Nyquist 近くまで上げるとそこでループが自己発振して音が全部潰れる。実効カットオフは 16kHz で頭打ちにした
- ゲートで切った鋸波は平均が 0 にならず、後段の `tanh` が片側だけ潰れる。出力に DC ブロッカーを一段入れた

### 鳴らすパターン

**実装済み**。16 ステップ・自然的短音階・1 拍目は常に主音。
2 ループごとに 1〜2 ステップだけ入れ替わる（音程・ゲート・アクセント・スライドのどれか）。R1 で丸ごと作り直す。

変化量は意図的に小さい。全部変えると別の曲になってしまって、ツマミを回している対象が入れ替わる。

---

## 未確認の項目（実機到着後に測る）

`probe.html` がこれを測るためのページ（v0.0.1 として公開していたもの。v0.1.0 では index から `probe` リンクで開ける）。

1. **可視領域** — パッドを下ろした状態で画面の下端が何 px の位置になるか。作業領域の高さがこれで確定する
2. **ボタン対応** — A→B→X→Y→L1→L2→R1→R2→Select→Start の順に押して index を記録。D-pad が `b12`〜`b15` で来るか `ax0/ax1` で来るか
3. **`mapping` の値** — `standard` か空文字か
4. **音の起動** — iOS Safari で `AudioContext` の起動にゲームパッドのボタン入力がユーザー操作として認められるか。認められなければ初回だけ画面タップさせる導線が要る

---

## 実装上の注意

- iOS Safari は Fullscreen API が使えない。ブラウザUIを消すにはホーム画面に追加して `display: standalone` の PWA にする
- `screen.orientation.lock('portrait')` も Safari では効かない。CSS 側で縦前提に割り切る
- 有線接続なので入力遅延は小さい。タイミングがシビアな操作も成立する
- iPhone Safari の Pages キャッシュは URL に `?v=NNN` を付ければ抜ける
- バージョンは `index.html` の JS 冒頭 `var VERSION` 一箇所で管理。タイトル行と `document.title` の両方に反映される

---

## 一覧ページへの掲載

**まだ載せない。**

一覧は冒頭で「全部リンクを開けばそのまま遊べます」と宣言している。
v0.1.0 はキーボードでも触れるようになったので「壊れて見える」状態は脱したが、
FlipPad の実機で手触りを確かめる前に載せると、直したくなったときに一覧の版まで動かすことになる。
実機で一度触ってから。

載せるときの案（既存の書式に合わせたもの）:

> **FLIP ACID** — 十字キーをつまみに見立て、押した方向でフィルターをひねるアシッド玩具。8BitDo FlipPad 用。
> `flip-acid`

カテゴリは「音と音楽」。テクノシーケンサーの隣。

同時に一覧ページ側のバージョンを v1.8.0 に上げ、件数表記（`21 titles`、`21件すべて公開中`）を 22 に直す必要がある。

---

## 次にやること

実機なしで進められる分は v0.1.0 で終わっている。残りは実機待ち。

1. **手触りの判断**。キーボード（↑↓←→ / Shift / Z / X / C）で Mac のブラウザでも同じように触れる。
   ツマミの効きが速すぎる・遅すぎるなら `index.html` の `K` テーブルの `rate` と `acc` を動かす
2. 実機到着後、下の未確認 4 項目を測る
3. ボタン index が確定したら `index.html` の `MAP` を直す（現在は standard mapping を仮置き）
4. パッドが覆う高さが確定したら CSS の `--pad` に入れる。中身は上に詰めてあるので、この値を入れるだけで収まる
5. 音が痩せる・うるさいと感じたら、後段サチュレーションの `tanh(y * 2.1) * 0.62` と
   低域補正 `1 + reso * 0.22` を触る。ここが一番キャラクターに効く

---

## デプロイ

`~/claude/flip-acid` にクローン済み。`gh` は `haristweet` で認証済み。
ワークスペース直下ではなく必ずこのディレクトリで `git push` すること。

ローカルで見るときは launch.json の `flip-acid`（ポート 8798）。
AudioWorklet は secure context でしか動かないので、`file://` で開くと音が出ない。
